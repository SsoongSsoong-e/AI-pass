# ============================================
# AI-Pass Server Environment Variables
# ============================================
# 
# 사용 방법:
# 1. 이 파일을 복사하여 .env 파일 생성
#    cp .env.example .env
# 
# 2. 각 환경 변수에 실제 값 입력
# 3. .env 파일은 Git에 커밋되지 않습니다 (.gitignore)
#
# ============================================

# --------------------------------------------
# 애플리케이션 환경
# --------------------------------------------
NODE_ENV=development

# --------------------------------------------
# PostgreSQL 데이터베이스 설정
# --------------------------------------------
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=ai_pass
DATABASE_USER=ai_pass
DATABASE_PASSWORD=secret

# --------------------------------------------
# MongoDB 설정
# --------------------------------------------
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DATABASE=ai-pass

# --------------------------------------------
# Google OAuth 설정
# --------------------------------------------
# Google Cloud Console에서 OAuth 2.0 클라이언트 ID 생성 필요
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=google_client_id
GOOGLE_CLIENT_SECRET=google_client_secret
GOOGLE_CALLBACK_URL=http://localhost:5173/api/auth/google/callback

# --------------------------------------------
# 시드 데이터 설정 (개발용)
# --------------------------------------------
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_USERNAME=admin

# --------------------------------------------
# AI 모델 서버 설정
# --------------------------------------------
# Docker 컨테이너에서 호스트의 모델 서버에 접근
# Mac/Windows: host.docker.internal 사용
# Linux: 172.17.0.1 또는 호스트 IP 사용
MODEL_SERVER_URL=http://host.docker.internal:5001

# --------------------------------------------
# 세션 설정
# --------------------------------------------
# SESSION_SECRET: 세션 암호화를 위한 시크릿 키
# 생성 방법: openssl rand -base64 32
# 값은 노션 댓글 참조
SESSION_SECRET=your-session-secret-key-here-please-refer-to-notion-comments
SESSION_MAX_AGE=604800
FRONTEND_URL=http://localhost:5173
